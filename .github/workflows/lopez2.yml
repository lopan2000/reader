name: Lopez2 Workflow

on:
  push:
    branches:
      - github-actions-tutorial
  workflow_dispatch:

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"